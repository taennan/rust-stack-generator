meta {
  name: create
  type: graphql
  seq: 2
}

post {
  url: {{coreGQLUrl}}
  body: graphql
  auth: inherit
}

body:graphql {
  mutation {
    create{entity_name}(input: {}) {
      id
      created
      updated
    }
  }
  
}
